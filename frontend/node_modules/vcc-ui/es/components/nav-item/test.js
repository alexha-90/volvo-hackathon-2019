function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; var ownKeys = Object.keys(source); if (typeof Object.getOwnPropertySymbols === 'function') { ownKeys = ownKeys.concat(Object.getOwnPropertySymbols(source).filter(function (sym) { return Object.getOwnPropertyDescriptor(source, sym).enumerable; })); } ownKeys.forEach(function (key) { _defineProperty(target, key, source[key]); }); } return target; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

import React from "react";
import { NavItem } from ".";
import volvoTheme from "../../themes/volvo";
import createSnapshot from "../../test/create-snapshot";
describe("<NavItem /> snapshot tests", function () {
  it("renders correctly with default props", function () {
    var snapshot = createSnapshot(React.createElement(NavItem, null, "Children"));
    expect(snapshot).toMatchSnapshot();
  });
  it("renders correctly with isDropdown=true", function () {
    var snapshot = createSnapshot(React.createElement(NavItem, {
      isDropdown: true
    }, "Children"));
    expect(snapshot).toMatchSnapshot();
  });
  it("renders correctly with isActive=true", function () {
    var snapshot = createSnapshot(React.createElement(NavItem, {
      isActive: true
    }, "Children"));
    expect(snapshot).toMatchSnapshot();
  });
  it("renders correctly with isDropdown=true and isActive=true", function () {
    var snapshot = createSnapshot(React.createElement(NavItem, {
      isDropdown: true,
      isActive: true
    }, "Children"));
    expect(snapshot).toMatchSnapshot();
  });
  it("renders correctly with theme", function () {
    var theme = _objectSpread({}, volvoTheme, {
      styles: {
        navItem: {
          backgroundColor: "green",
          padding: "20px",
          color: "black"
        },
        navItemArrows: {
          color: "white",
          size: "16"
        }
      }
    });

    var snapshot = createSnapshot(React.createElement(NavItem, {
      isDropdown: true,
      isActive: true
    }, "Children"), theme);
    expect(snapshot).toMatchSnapshot();
  });
});