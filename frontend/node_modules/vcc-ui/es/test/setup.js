var jsdom = require("jsdom");

var matchMedia = require("./mocks/matchMedia");

var raf = require("./mocks/raf");

var JSDOM = jsdom.JSDOM;

var _ref = new JSDOM("<!doctype html><html><body></body></html>"),
    window = _ref.window;

global.window = window;
global.window.matchMedia = window.matchMedia || matchMedia;
global.document = window.document;
global.navigator = window.navigator;
global.requestAnimationFrame = raf;
/* eslint-disable-next-line no-console */

var warn = console.warn;
global.console = {
  warn: function warn() {},
  error: function error() {
    return warn.apply(void 0, arguments);
  }
};