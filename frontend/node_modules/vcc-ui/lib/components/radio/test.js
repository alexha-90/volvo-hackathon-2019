"use strict";

var _react = _interopRequireDefault(require("react"));

var _ = require(".");

var _createSnapshot = _interopRequireDefault(require("../../test/create-snapshot"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var onChange = function onChange() {};

describe("<Radio /> snapshot tests", function () {
  it("renders <Radio /> correctly with default props", function () {
    var snapshot = (0, _createSnapshot.default)(_react.default.createElement(_.Radio, {
      name: "test",
      checked: false,
      onChange: onChange
    }));
    expect(snapshot).toMatchSnapshot();
  });
  it("renders <Radio /> correctly with checked prop", function () {
    var snapshot = (0, _createSnapshot.default)(_react.default.createElement(_.Radio, {
      name: "test",
      checked: true,
      onChange: onChange
    }));
    expect(snapshot).toMatchSnapshot();
  });
  it("renders <Radio /> without overriding styles", function () {
    var snapshot = (0, _createSnapshot.default)(_react.default.createElement(_.Radio, {
      name: "test",
      checked: false,
      extend: {
        display: "none"
      },
      onChange: onChange
    }));
    expect(snapshot).toMatchSnapshot();
  });
});