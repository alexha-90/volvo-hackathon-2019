"use strict";

var _react = _interopRequireDefault(require("react"));

var _createSnapshot = _interopRequireDefault(require("../../test/create-snapshot"));

var _volvo = _interopRequireDefault(require("../../themes/volvo"));

var _ = require(".");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; var ownKeys = Object.keys(source); if (typeof Object.getOwnPropertySymbols === 'function') { ownKeys = ownKeys.concat(Object.getOwnPropertySymbols(source).filter(function (sym) { return Object.getOwnPropertyDescriptor(source, sym).enumerable; })); } ownKeys.forEach(function (key) { _defineProperty(target, key, source[key]); }); } return target; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

describe("<LoadingBar /> snapshot tests", function () {
  it("renders nothing without isLoading prop", function () {
    var snapshot = (0, _createSnapshot.default)(_react.default.createElement(_.LoadingBar, null));
    expect(snapshot).toMatchSnapshot();
  });
  it("renders correctly when isLoading is true", function () {
    var snapshot = (0, _createSnapshot.default)(_react.default.createElement(_.LoadingBar, {
      isLoading: true
    }));
    expect(snapshot).toMatchSnapshot();
  });
  it("renders correctly with default theme", function () {
    var snapshot = (0, _createSnapshot.default)(_react.default.createElement(_.LoadingBar, {
      isLoading: true
    }), _volvo.default);
    expect(snapshot).toMatchSnapshot();
  });
  it("renders correctly with default theme & direction = rtl", function () {
    var theme = _objectSpread({}, _volvo.default, {
      direction: "rtl"
    });

    var snapshot = (0, _createSnapshot.default)(_react.default.createElement(_.LoadingBar, {
      isLoading: true
    }), theme);
    expect(snapshot).toMatchSnapshot();
  });
});