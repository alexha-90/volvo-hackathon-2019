"use strict";

var _react = _interopRequireDefault(require("react"));

var _ = require(".");

var _createSnapshot = _interopRequireDefault(require("../../test/create-snapshot"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe("<Arrow /> snapshot tests", function () {
  it("renders <Arrow/> correctly with direction=right ", function () {
    var snapshot = (0, _createSnapshot.default)(_react.default.createElement(_.Arrow, {
      direction: "right"
    }));
    expect(snapshot).toMatchSnapshot();
  });
  it("renders <Arrow/> correctly with direction=down ", function () {
    var snapshot = (0, _createSnapshot.default)(_react.default.createElement(_.Arrow, {
      direction: "down"
    }));
    expect(snapshot).toMatchSnapshot();
  });
  it("renders <Arrow/> correctly with direction=left ", function () {
    var snapshot = (0, _createSnapshot.default)(_react.default.createElement(_.Arrow, {
      direction: "left"
    }));
    expect(snapshot).toMatchSnapshot();
  });
  it("render <Arrow /> correctly with theme", function () {
    var theme = {
      nav: {
        fill: "pink"
      }
    };
    var snapshot = (0, _createSnapshot.default)(_react.default.createElement(_.Arrow, {
      direction: "left"
    }), theme);
    expect(snapshot).toMatchSnapshot();
  });
});