"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = createSnapshot;

var _jestReactFela = require("jest-react-fela");

var _felaMonolithic = _interopRequireDefault(require("fela-monolithic"));

var _fela = require("fela");

var _styleProvider = require("../style-provider");

var _themeProvider = require("../theme-provider");

var _styleRenderer = require("../style-renderer");

var _volvo = _interopRequireDefault(require("../themes/volvo"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _objectSpread(target) { for (var i = 1; i < arguments.length; i++) { var source = arguments[i] != null ? arguments[i] : {}; var ownKeys = Object.keys(source); if (typeof Object.getOwnPropertySymbols === 'function') { ownKeys = ownKeys.concat(Object.getOwnPropertySymbols(source).filter(function (sym) { return Object.getOwnPropertyDescriptor(source, sym).enumerable; })); } ownKeys.forEach(function (key) { _defineProperty(target, key, source[key]); }); } return target; }

function _defineProperty(obj, key, value) { if (key in obj) { Object.defineProperty(obj, key, { value: value, enumerable: true, configurable: true, writable: true }); } else { obj[key] = value; } return obj; }

function getMonolithicRenderer() {
  return (0, _fela.enhance)((0, _felaMonolithic.default)())(_styleRenderer.styleRenderer)();
} // The snapshot will try to pick up named classnames from this list
// if it happens to find a matching named key from the theme.styles


var components = ["arrow", "button", "checkbox", "icon", "link", "logo", "nav", "navItem", "selectInput", "selectInputValue", "selectInputOptions", "selectInputOption", "slider", "sliderTrack", "sliderThumb", "spinner", "tabNav", "tabNavItem", "textInput", "toggle"];

function addClassNameToTheme(theme, name) {
  if (!theme.styles) {
    theme.styles = {};
  }

  if (!theme.styles[name]) {
    theme.styles[name] = {};
  }

  theme.styles[name].className = name;
  return theme;
}

var baseTheme = _objectSpread({}, _volvo.default, {
  logoImagesPath: "/logo/testpath/",
  fontsPath: "/fonts/testpath/"
});

function createSnapshot(component) {
  var theme = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : baseTheme;
  components.forEach(function (component) {
    return addClassNameToTheme(theme, component);
  });
  return (0, _jestReactFela.createSnapshot)(component, theme, getMonolithicRenderer(), _styleProvider.StyleProvider, _themeProvider.ThemeProvider);
}