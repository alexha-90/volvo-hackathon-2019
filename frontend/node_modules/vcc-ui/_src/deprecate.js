const isProduction = process.env.NODE_ENV === "production";
const cache = {};

export function deprecate(message, condition = true) {
	if (!isProduction && !cache[message] && condition) {
		cache[message] = true;
		/* eslint-disable no-console */
		console.warn(message);
		/* eslint-enable */
	}
}
